# VITE_API_URL: The backend AI API URL (e.g. your deployed Render service URL).
# Leave blank during local development to use local server.
VITE_API_URL="https://roadguard-api.onrender.com"

# Server Port (optional, defaults to 3000 locally or provided by Render in cloud)
PORT=3000
